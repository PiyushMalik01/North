import DriftView from '@/components/drift/DriftView';

export const metadata = {
  title: 'drift — North',
  description: 'What is moving in your field — news, market shifts, peer wins, events.',
};

export default function DriftPage() {
  return <DriftView />;
}
