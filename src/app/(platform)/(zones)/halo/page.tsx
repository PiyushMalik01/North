import { HaloView } from '@/components/halo/HaloView';

export const metadata = {
  title: 'halo — North',
  description: 'Your skill record, gleams, and activity trail.',
};

export default function HaloPage() {
  return <HaloView />;
}
