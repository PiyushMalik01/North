import PerchView from '@/components/perch/PerchView';

export const metadata = {
  title: 'perch — North',
  description: 'The standings. See where you rank.',
};

export default function PerchPage() {
  return <PerchView />;
}
