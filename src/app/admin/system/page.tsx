import SystemPanel from '@/components/admin/SystemPanel';

export const metadata = {
  title: 'System — North Admin',
  description: 'Feature flags and platform health.',
};

export default function SystemPage() {
  return <SystemPanel />;
}
