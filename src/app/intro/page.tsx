import { StoryMode } from '@/components/intro/StoryMode';

export const metadata = {
  title: 'North — How a penguin went north',
  description: 'Meet Nor. The origin of your guide.',
};

export default function IntroPage() {
  return <StoryMode />;
}
