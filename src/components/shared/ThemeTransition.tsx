'use client';

// View Transitions API now handles theme transitions via ThemeToggle.
// This component is kept as a no-op for import compatibility.
export function ThemeTransition() {
  return null;
}
